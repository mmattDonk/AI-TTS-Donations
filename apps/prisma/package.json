{
  "name": "@solrock/prisma",
  "version": "1.0.0",
  "description": "the database schema for solrock",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "./update_schema.sh typescript && prisma generate",
    "python": "./update_schema.sh python && source .venv/bin/activate && python3 -m prisma generate"
  },
  "keywords": [],
  "author": "mmattbtw <matt@mmatt.net>",
  "license": "AGPL-3",
  "devDependencies": {
    "prisma": "^4.3.1",
    "typescript": "4.7.4"
  },
  "dependencies": {
    "@prisma/client": "^4.3.1"
  }
}
